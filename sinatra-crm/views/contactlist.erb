<div class="contactcount">
	<h2>There are <%=@contact_count %> contacts</h2>
</div>

<div class="contactwrapper">
	<% @contact_list.each do |contact| %>
	<div class="contactribbon">
		<%= contact.full_name%> (<%= contact.email %>)
		<div>
			<a href="#">edit</a>
			<a href="#">delete</a>
		</div> 
	</div>
	<% end %>
</div>

